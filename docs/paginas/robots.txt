# IBLO Eventos
User-agent: *
# La API devuelve JSON, no páginas: no hay nada que indexar ahí.
Disallow: /api/
Allow: /

# El panel del dueño NO se bloquea acá a propósito: se marca con
# «X-Robots-Tag: noindex» en _headers, y para leer esa marca Google
# primero tiene que poder entrar. Bloqueándolo acá nunca la vería y la
# dirección podía aparecer igual en el buscador, pelada.

Sitemap: https://iblo-eventos.pages.dev/sitemap.xml

# El archivo de 32 digitos que hay en la raiz es la clave de IndexNow, con
# la que se le avisa a Bing, Yandex, Seznam y Naver que el sitio cambio.
# No es un secreto: publicarla ES la prueba de que el dominio es nuestro.
# Se manda con ./avisar-buscadores.sh. Google no usa IndexNow: para Google
# la via sigue siendo el sitemap de arriba.
