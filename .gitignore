# La LLAVE nunca al repo: init deja la marca y la credencial donde se corre.
# El cliente sí es nuestro y va commiteado.
.rezona/
__pycache__/

# la clave con que se firma el APK: nunca al repo
*.keystore
*.jks
