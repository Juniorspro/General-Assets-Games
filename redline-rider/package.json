{
  "name": "redline-rider",
  "version": "1.0.0",
  "private": true,
  "description": "Redline Rider — first-person motorcycle highway racer (three.js)",
  "scripts": {
    "build": "node build.mjs",
    "cdn": "node build.mjs --cdn",
    "single": "node build.mjs --single",
    "dev": "node build.mjs --dev",
    "serve": "python3 -m http.server 8090"
  },
  "devDependencies": { "esbuild": "^0.25.0" }
}
