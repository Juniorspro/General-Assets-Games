<?xml version="1.0" encoding="utf-8"?>
<resources>
  <!-- ── SIN BARRA Y CON EL FONDO DEL PROPIO LAUNCHER ──
       `windowShowWallpaper` deja ver el fondo de pantalla del sistema por
       detrás; acá NO se quiere, porque el fondo lo dibuja el propio launcher y
       si no se verían los dos. El color de arranque es el azul del cielo, así
       que el primer cuadro antes de que el WebView pinte ya es del color justo
       en vez de un flash blanco. -->
  <style name="TemaAero" parent="@android:style/Theme.Material.NoActionBar">
    <item name="android:windowBackground">#0a3d6b</item>
    <item name="android:windowNoTitle">true</item>
    <item name="android:windowFullscreen">false</item>
    <item name="android:windowDrawsSystemBarBackgrounds">true</item>
    <item name="android:statusBarColor">@android:color/transparent</item>
    <item name="android:navigationBarColor">@android:color/transparent</item>
    <item name="android:windowLightStatusBar">false</item>
    <item name="android:windowAnimationStyle">@null</item>
  </style>
</resources>
