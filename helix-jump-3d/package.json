{
  "name": "helix-jump-3d",
  "version": "1.0.0",
  "private": true,
  "description": "Helix Jump 3D — three.js remake",
  "scripts": {
    "build": "node build.mjs",
    "dev": "node build.mjs --dev",
    "serve": "python3 -m http.server 8080"
  },
  "devDependencies": { "esbuild": "^0.25.0" }
}
